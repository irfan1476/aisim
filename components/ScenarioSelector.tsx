import { Check, Factory } from 'lucide-react';
import { scenarioList } from '../lib/scenarios/registry';
import type { CurrencyMode } from '../lib/scenarios/types';

interface Props { enabled: boolean; scenarioId: string; currency: CurrencyMode; onEnabledChange: (enabled: boolean) => void; onScenarioChange: (scenarioId: string) => void; onCurrencyChange: (currency: CurrencyMode) => void }

export default function ScenarioSelector({ enabled, scenarioId, currency, onEnabledChange, onScenarioChange, onCurrencyChange }: Props) {
  const scenario = scenarioList.find((item) => item.id === scenarioId) || scenarioList[0];
  return <section className="mt-5 rounded-2xl border border-ink/10 bg-white p-5"><div className="flex items-start justify-between gap-4"><div><p className="text-xs font-bold uppercase tracking-widest text-gold">Optional scenario mode</p><h2 className="mt-2 text-xl font-bold">Add a live operating challenge</h2><p className="mt-1 text-sm leading-6 text-ink/55">Keep standard play unchanged, or test your strategy against a domain-specific operating environment.</p></div><button type="button" aria-pressed={enabled} onClick={() => onEnabledChange(!enabled)} className={`flex h-8 w-14 items-center rounded-full p-1 transition ${enabled ? 'bg-emerald' : 'bg-ink/15'}`}><span className={`flex h-6 w-6 items-center justify-center rounded-full bg-white shadow transition ${enabled ? 'translate-x-6' : ''}`}>{enabled && <Check size={14} className="text-emerald"/>}</span></button></div>{enabled && <div className="mt-5 rounded-xl border border-gold/25 bg-gold/5 p-4"><div className="flex flex-wrap items-center gap-2"><Factory size={17} className="text-gold"/><label htmlFor="scenario-select" className="sr-only">Choose scenario</label><select id="scenario-select" value={scenario.id} onChange={(event) => onScenarioChange(event.target.value)} className="rounded-lg border border-ink/10 bg-white px-2 py-1 text-sm font-bold"><option value={scenario.id}>{scenario.name}</option>{scenarioList.filter((item) => item.id !== scenario.id).map((item) => <option key={item.id} value={item.id}>{item.name}</option>)}</select><span className="rounded-full bg-gold/15 px-2 py-1 text-[10px] font-bold text-ink/60">{scenario.difficulty}</span></div><p className="mt-2 text-sm leading-6 text-ink/60">{scenario.description}</p><div className="mt-4 grid gap-2 sm:grid-cols-2">{scenario.challenges.map((challenge) => <div key={challenge.id} className="rounded-lg bg-white/70 p-3 text-xs"><b>{challenge.label}</b><span className="ml-1 text-gold">{challenge.severity}</span><p className="mt-1 text-ink/50">{challenge.description}</p></div>)}</div><div className="mt-4 flex flex-wrap items-center justify-between gap-3"><span className="text-xs font-bold text-ink/60">Quarterly budget: {currency === '$' ? '$5M' : '₹5 Cr'}</span><div className="flex gap-2"><button type="button" onClick={() => onCurrencyChange('$')} className={`rounded-lg border px-3 py-2 text-xs font-bold ${currency === '$' ? 'border-emerald bg-emerald/10' : 'border-ink/10'}`}>$ USD</button><button type="button" onClick={() => onCurrencyChange('₹')} className={`rounded-lg border px-3 py-2 text-xs font-bold ${currency === '₹' ? 'border-emerald bg-emerald/10' : 'border-ink/10'}`}>₹ INR</button></div></div><p className="mt-3 text-[11px] text-ink/45">These pressures shape the campaign; their consequences emerge through decisions and crises.</p></div>}{!enabled && <div className="mt-4 flex flex-wrap items-center justify-between gap-3 border-t border-ink/8 pt-4"><span className="text-xs text-ink/50">Standard budget framing: {currency === '$' ? '$10M' : '₹10 Cr'}</span><div className="flex gap-2"><button type="button" onClick={() => onCurrencyChange('$')} className={`rounded-lg border px-3 py-2 text-xs font-bold ${currency === '$' ? 'border-gold bg-gold/10' : 'border-ink/10'}`}>$ USD</button><button type="button" onClick={() => onCurrencyChange('₹')} className={`rounded-lg border px-3 py-2 text-xs font-bold ${currency === '₹' ? 'border-gold bg-gold/10' : 'border-ink/10'}`}>₹ INR</button></div></div>}</section>;
}
