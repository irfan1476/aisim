'use client';
import {useMemo,useState} from 'react';
import {Activity,BarChart3,BrainCircuit,GitBranch,LineChart,ShieldCheck,Sparkles,Target,TrendingUp,X} from 'lucide-react';
import CampaignTools from './CampaignTools';

type Props={state:any; initiatives:any[]};
const benchmarks={roi:35,adoption:67,data:80,satisfaction:75,efficiency:55,risk:20};
export default function AnalyticsHub({state,initiatives}:Props){
 const [open,setOpen]=useState(false);const [tab,setTab]=useState('live');const [whatIf,setWhatIf]=useState(false);const [alternative,setAlternative]=useState<string[]>([]);const [result,setResult]=useState<any>(null);
 const history=useMemo(()=>state.history||[],[state.history]);const current=state;const previous=history.length>1?history[history.length-2].metrics:null;
 const score=(v:number,b:number)=>v>=b?'text-emerald':'text-crimson';
 const toggle=(id:string)=>setAlternative(a=>a.includes(id)?a.filter(x=>x!==id):a.length<3?[...a,id]:a);
 const runWhatIf=async()=>{const res=await fetch('/api/game/whatif',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({current:state.alloc,alternative:{...state.alloc,people:Math.min(50,state.alloc.people+5),compliance:Math.min(50,state.alloc.compliance+5)},initiatives:alternative})});setResult(await res.json())};
 const trends=useMemo(()=>{const values=history.map((h:any)=>h.metrics?.roi??0);return values.length?values:[0]},[history]);
 return <><button aria-label="Open analytics" onClick={()=>setOpen(true)} className="fixed bottom-5 right-5 z-30 flex items-center gap-2 rounded-xl border border-emerald bg-ink px-4 py-3 text-xs font-bold text-white shadow-xl"><Activity size={16} className="text-emerald"/> Analytics <span className="rounded bg-emerald px-2 py-0.5 text-ink">Q{state.q}/12</span></button>{open&&<div className="fixed inset-0 z-40 bg-ink/35" onClick={()=>setOpen(false)}><aside role="dialog" aria-label="Insights and analytics" onClick={e=>e.stopPropagation()} className="absolute right-0 top-0 flex h-full w-full max-w-[480px] flex-col bg-ink text-white shadow-2xl"><header className="flex items-center justify-between border-b border-white/10 p-5"><div><p className="text-[10px] font-bold uppercase tracking-[.2em] text-emerald">Always-on intelligence</p><h2 className="mt-1 text-xl font-bold">Insights & analytics</h2></div><button onClick={()=>setOpen(false)} className="rounded-lg p-2 text-white/50 hover:bg-white/10 hover:text-white"><X size={18}/></button></header><nav className="grid grid-cols-4 border-b border-white/10">{[['live','Live'],['trends','Trends'],['diagnostics','Diagnostics'],['learn','Learn']].map(([id,label])=><button key={id} onClick={()=>setTab(id)} className={`border-b-2 px-2 py-3 text-[10px] font-bold uppercase tracking-wider ${tab===id?'border-emerald text-emerald':'border-transparent text-white/45'}`}>{label}</button>)}</nav><div className="flex-1 overflow-y-auto p-5">{tab==='live'&&<div className="space-y-5"><div className="grid grid-cols-2 gap-3">{[['ROI',current.roi,benchmarks.roi],['Adoption',current.adoption,benchmarks.adoption],['Data readiness',current.data,benchmarks.data],['Employee trust',current.satisfaction,benchmarks.satisfaction]].map(([label,value,benchmark])=><div key={label as string} className="rounded-xl border border-white/10 bg-white/5 p-4"><p className="text-[10px] uppercase tracking-wider text-white/45">{label as string}</p><p className={`mt-2 text-2xl font-bold ${score(value as number,benchmark as number)}`}>{(value as number).toFixed(label==='ROI'?1:0)}%</p><div className="mt-3 h-1.5 rounded-full bg-white/10"><div className="h-full rounded-full bg-emerald" style={{width:`${Math.min(100,(value as number)/(benchmark as number)*100)}%`}}/></div><p className="mt-2 text-[10px] text-white/40">Benchmark {benchmark}%</p></div>)}</div><div className="rounded-xl border border-white/10 bg-white/5 p-4"><div className="flex items-center gap-2"><Target size={16} className="text-emerald"/><b>Traffic-light scorecard</b></div><div className="mt-4 space-y-3">{[['Risk exposure',current.risk,20],['Efficiency',current.efficiency,55],['AI literacy',current.literacy,70],['Satisfaction',current.satisfaction,75]].map(([label,value,b])=><div key={label as string} className="flex items-center justify-between text-sm"><span className="text-white/65">{label as string}</span><span className={`font-bold ${(label==='Risk exposure'?(value as number)<(b as number):(value as number)>=(b as number))?'text-emerald':'text-gold'}`}>{(value as number).toFixed(0)}% · {previous?(value as number)-(previous[label==='Risk exposure'?'risk':label==='Efficiency'?'efficiency':label==='AI literacy'?'literacy':'satisfaction']??value):0>=0?'up':'flat'}</span></div>)}</div></div><button onClick={()=>setWhatIf(true)} className="flex w-full items-center justify-center gap-2 rounded-xl bg-emerald px-4 py-3 text-sm font-bold text-ink"><GitBranch size={16}/> Run What-If Analysis</button></div>}
 {tab==='trends'&&<div className="space-y-5"><div className="rounded-xl border border-white/10 bg-white/5 p-4"><div className="flex items-center gap-2"><LineChart size={16} className="text-emerald"/><b>ROI trend by quarter</b></div><div className="mt-5 flex h-32 items-end gap-2">{trends.map((v:number,i:number)=><div key={i} className="group flex flex-1 flex-col items-center gap-1"><div className="w-full rounded-t bg-emerald/80 transition group-hover:bg-emerald" style={{height:`${Math.max(5,Math.min(100,v))}%`}}/><span className="text-[9px] text-white/40">Q{i+1}</span></div>)}</div></div><div className="rounded-xl border border-white/10 bg-white/5 p-4"><div className="flex items-center gap-2"><BarChart3 size={16} className="text-emerald"/><b>Current vs benchmark</b></div><div className="mt-4 space-y-3">{[['ROI',current.roi,35],['Adoption',current.adoption,67],['Data',current.data,80],['Risk',100-current.risk,80]].map(([l,v,b])=><div key={l as string}><div className="flex justify-between text-xs"><span className="text-white/60">{l as string}</span><span>{(v as number).toFixed(0)} / {b as number}</span></div><div className="mt-1 h-2 rounded bg-white/10"><div className="h-full rounded bg-emerald" style={{width:`${Math.min(100,(v as number)/(b as number)*100)}%`}}/></div></div>)}</div></div><p className="text-xs leading-5 text-white/45">Trends update after every submitted quarter and crisis response. More history will make the pattern more reliable.</p></div>}
 {tab==='diagnostics'&&<div className="space-y-5"><div className="rounded-xl border border-white/10 bg-white/5 p-4"><div className="flex items-center gap-2"><ShieldCheck size={16} className="text-emerald"/><b>Risk / reward profile</b></div><div className="mt-5 flex items-end gap-3"><div className="h-28 w-28 rounded-full border-4 border-emerald/30 p-5 text-center"><b className="text-2xl">{(current.roi/Math.max(current.risk,1)).toFixed(1)}</b><p className="text-[9px] text-white/45">risk-adjusted</p></div><div className="text-sm text-white/65">You are taking <b className="text-white">{current.risk.toFixed(0)}%</b> risk for <b className="text-emerald">{current.roi.toFixed(1)}%</b> ROI.</div></div></div><div className="rounded-xl border border-white/10 bg-white/5 p-4"><div className="flex items-center gap-2"><UsersIcon/><b>Human Capital Index</b></div><p className="mt-3 text-4xl font-bold text-emerald">{((current.adoption*.35+current.satisfaction*.25+current.literacy*.25+current.alloc.people*4*.15)).toFixed(0)}<span className="text-sm text-white/45"> / 100</span></p><div className="mt-4 space-y-2 text-xs text-white/60"><p>Adoption <b className="float-right text-white">{current.adoption.toFixed(0)}%</b></p><p>Employee satisfaction <b className="float-right text-white">{current.satisfaction.toFixed(0)}%</b></p><p>AI literacy <b className="float-right text-white">{current.literacy.toFixed(0)}%</b></p><p>People budget <b className="float-right text-white">{current.alloc.people}%</b></p></div></div><div className="rounded-xl border border-gold/25 bg-gold/10 p-4 text-sm leading-6 text-white/70"><b className="text-gold">No-loss diagnosis:</b> {current.risk>30?'Governance is the current learning edge. Increase compliance before scaling high-risk initiatives.':'Your risk posture is improving. The next diagnostic is whether adoption can keep pace with ambition.'}</div></div>}
 {tab==='learn'&&<div className="space-y-5"><div className="rounded-xl border border-white/10 bg-white/5 p-4"><div className="flex items-center gap-2"><BrainCircuit size={16} className="text-emerald"/><b>Learning progress</b></div><p className="mt-3 text-3xl font-bold">Q{current.q} <span className="text-sm font-normal text-white/45">of 12</span></p><p className="mt-2 text-sm leading-6 text-white/60">Your strategy is evolving through consequences, not just instructions. Keep watching the relationship between people investment and ROI.</p></div><div className="rounded-xl border border-white/10 bg-white/5 p-4"><div className="flex items-center gap-2"><Sparkles size={16} className="text-emerald"/><b>Decision evolution</b></div><div className="mt-4 flex flex-wrap gap-2">{history.slice(-5).map((h:any)=><span key={h.q} className="rounded-md bg-emerald/15 px-2 py-1 text-[10px] text-emerald">Q{h.q}: {h.chosen?.length||0} bets</span>)}</div><p className="mt-4 text-xs leading-5 text-white/45">Baseline assessment: {state.baseline?.length?Math.round(state.baseline.reduce((a:number,b:number)=>a+b,0)/state.baseline.length*20):'—'}% confidence signal.</p></div><div className="rounded-xl border border-white/10 bg-white/5 p-4 text-sm"><b>Benchmark snapshot</b><div className="mt-3 space-y-2 text-white/60"><p>Google Cloud ROI benchmark <span className="float-right text-white">35%</span></p><p>BCG adoption benchmark <span className="float-right text-white">67%</span></p><p>Current overall position <span className="float-right text-emerald">{current.score>70?'ahead':'building'}</span></p></div></div></div>}<CampaignTools state={state}/></div></aside></div>}{whatIf&&<div className="fixed inset-0 z-50 flex items-center justify-center bg-ink/60 p-4" onClick={()=>setWhatIf(false)}><div onClick={e=>e.stopPropagation()} className="w-full max-w-2xl rounded-2xl bg-white p-6 text-ink"><div className="flex items-center justify-between"><div><p className="text-xs font-bold uppercase tracking-widest text-emerald">Decision laboratory</p><h3 className="mt-1 text-2xl font-bold">What if you changed the portfolio?</h3></div><button onClick={()=>setWhatIf(false)}><X size={18}/></button></div><p className="mt-3 text-sm text-ink/55">Select an alternative set of initiatives. The analysis uses your current allocation and does not change the live campaign.</p><div className="mt-5 grid gap-2 sm:grid-cols-2">{initiatives.map(i=><button key={i.id} onClick={()=>toggle(i.id)} className={`rounded-lg border p-3 text-left text-sm ${alternative.includes(i.id)?'border-emerald bg-emerald/10':'border-ink/10 bg-mist'}`}><b>{i.name}</b><span className="ml-2 text-ink/45">{i.roi}% ROI</span></button>)}</div><div className="mt-5 flex gap-3"><button disabled={!alternative.length} onClick={runWhatIf} className="rounded-lg bg-ink px-4 py-3 text-sm font-bold text-white disabled:opacity-35">Run analysis</button><button onClick={()=>setWhatIf(false)} className="rounded-lg border border-ink/15 px-4 py-3 text-sm font-bold">Close</button></div>{result&&<div className="mt-5 rounded-xl bg-mist p-4"><p className="text-sm font-bold">Comparison insight</p>{result.differences?.map((d:any)=><p key={d.metric} className="mt-2 text-sm text-ink/65"><b>{d.metric}:</b> {d.delta>0?'+':''}{d.delta} · {d.explanation}</p>)}<p className="mt-3 text-sm leading-6 text-ink/70">{result.recommendation}</p></div>}</div></div>}</>}
function UsersIcon(){return <span className="inline-flex h-4 w-4 items-center justify-center rounded-full border border-emerald text-[9px] text-emerald">+</span>}
